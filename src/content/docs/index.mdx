---
title: Sluiceway
description: "Sluiceway is a GitHub Action: one issue shows which Pulumi, OpenTofu, Helm and Kubernetes manifests stacks have changes waiting, and a tick deploys that stack."
template: splash
# The browser title says what the product is, without Starlight's " | Sluiceway" suffix.
head:
  - tag: title
    content: "Sluiceway: a deploy dashboard in one GitHub issue"
hero:
  title: One issue. Every stack. <span class="sw-swash">Your tick.</span>
  actions:
    - text: Get started
      link: /get-started/
    - text: What it looks like
      link: /what-it-looks-like/
---

import Card from "../../components/Card.astro";
import HeaderPicture from "../../components/HeaderPicture.astro";
import HostedLine from "../../components/landing/HostedLine.astro";
import SpeechBubble from "../../components/SpeechBubble.astro";
import Steps from "../../components/Steps.astro";
import WaveRule from "../../components/WaveRule.astro";
import { goodNewsLine, howItWorks } from "../../lib/action-text";
import { page, ROUTES } from "../../lib/routes";
import "../../styles/landing.css";

<div class="sw-landing">

<section class="sw-landing__calm not-content" aria-label="Every stack in sync">
  <SpeechBubble line={goodNewsLine()} />
  <HeaderPicture
    picture="in-sync"
    alt="Penny, the sluice gate, resting on a calm quay because every stack is in sync"
  />
  <p class="sw-landing__small">{"The panel above shows crates waiting. This picture shows nothing pending. The header of your dashboard always shows the real state of your stacks."}</p>
</section>

## How it works

<Steps items={howItWorks()} html />

<WaveRule />

## Where to go next

<div class="sw-card-grid">
  <Card title="Get started" href={ROUTES.getStarted} bar>
    Add Sluiceway one step at a time, starting with a check that changes nothing.
  </Card>
  <Card title="Configuration" href={ROUTES.configuration} tilt="left">
    Every key of `sluiceway.yaml` and what it changes.
  </Card>
  <Card title="Why" href={ROUTES.why} tilt="right">
    The decision records say why Sluiceway works the way it does.
  </Card>
</div>

<p class="sw-landing__guides">
  The guides: <a href={page("how-it-works")}>How it works</a>, <a href={page("guides/workflow")}>The workflow</a>, <a href={page("guides/configuration")}>Configuration</a>, <a href={page("guides/credentials")}>Credentials</a>, <a href={page("guides/example-workflows")}>Example workflows</a>, <a href={page("guides/notifications")}>Notifications and metrics</a> and <a href={page("guides/security")}>Security</a>. Every key is in the <a href={page("reference/sluiceway-yaml")}><code>sluiceway.yaml</code> reference</a>.
</p>

<HostedLine />

</div>
