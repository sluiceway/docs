---
title: Sluiceway
description: "A GitHub Action: one issue shows which Pulumi, OpenTofu, Terraform, Helm or Kubernetes stacks have changes waiting, and a tick deploys that stack."
template: splash
# The browser title says what the product is, without Starlight's " | Sluiceway" suffix.
head:
  - tag: title
    content: "Sluiceway: a deploy dashboard in one GitHub issue"
hero:
  title: One issue. Every stack. <span class="sw-swash">Your tick.</span>
  actions:
    - text: Get started
      link: /get-started/
    - text: What it looks like
      link: /what-it-looks-like/
---

import Card from "../../components/Card.astro";
import HeaderPicture from "../../components/HeaderPicture.astro";
import HostedLine from "../../components/landing/HostedLine.astro";
import SpeechBubble from "../../components/SpeechBubble.astro";
import Steps from "../../components/Steps.astro";
import WaveRule from "../../components/WaveRule.astro";
import { goodNewsLine, howItWorks } from "../../lib/action-text";
import { page, ROUTES } from "../../lib/routes";
import "../../styles/landing.css";

<div class="sw-landing">

## How it works

<Steps items={howItWorks()} html />

{/* The end of the loop: the hero's crates have gone through, and Penny rests. */}
<section class="sw-landing__calm not-content" aria-label="Every stack in sync">
  <SpeechBubble line={goodNewsLine()} />
  <HeaderPicture
    picture="in-sync"
    alt="Penny, the sluice gate, resting on a calm quay because every stack is in sync"
  />
</section>

<WaveRule />

## Where to go next

<div class="sw-card-grid">
  <Card title="Get started" href={ROUTES.getStarted} bar>
    Add Sluiceway one step at a time, starting with a check that changes nothing.
  </Card>
  <Card title="Credentials" href={ROUTES.credentials} tilt="left">
    Load your cloud and state credentials on Sluiceway's step, or in a step right before it.
  </Card>
  <Card title="Using the dashboard" href={ROUTES.usingTheDashboard} tilt="right">
    Read the rows, tick a box to deploy a stack, and read the job log.
  </Card>
</div>

<div class="sw-landing__guides">
  <p>The guides, in order:</p>
  <ul>
    <li><a href={page("guides/workflow")}>The workflow</a></li>
    <li><a href={page("guides/init")}>Start with init</a></li>
    <li><a href={page("guides/read-only-trial")}>Start read only</a></li>
    <li><a href={page("guides/credentials")}>Credentials</a></li>
    <li><a href={page("guides/configuration")}>Configuration</a></li>
    <li><a href={page("guides/example-workflows")}>Example workflows</a></li>
    <li><a href={page("guides/split-workflow")}>The split workflow</a></li>
    <li><a href={page("guides/notifications")}>Notifications and metrics</a></li>
    <li><a href={page("guides/security")}>Security</a></li>
  </ul>
  <p>Every key is in the <a href={page("reference/sluiceway-yaml")}><code>sluiceway.yaml</code> reference</a>. <a href={page("reference/what-sluiceway-writes")}>What Sluiceway writes</a> documents the markers, the deployment records and the result file for a script or an agent that reads them. The <a href={ROUTES.why}>decision records</a> say why Sluiceway works the way it does.</p>
</div>

<HostedLine />

</div>
