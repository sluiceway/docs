name: pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

# One deploy at a time. A deploy that has started runs to the end; the next one waits.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # The address comes from the repository variables DOCS_SITE and DOCS_BASE. Unset, the site
    # is built for https://sluiceway.github.io/docs/.
    env:
      DOCS_SITE: ${{ vars.DOCS_SITE || 'https://sluiceway.github.io' }}
      DOCS_BASE: ${{ vars.DOCS_BASE || '/docs' }}
    steps:
      # fetch-depth 0 fetches the submodule's tags, which the build reads the pin from.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: true
          fetch-depth: 0

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: .bun-version

      - run: bun install --frozen-lockfile

      # The same checks as ci.yml, so a red main never deploys.
      - run: bun run check

      - run: bun run links

      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5.0.1
